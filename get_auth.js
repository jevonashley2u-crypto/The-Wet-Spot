import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; import ws from 'ws'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY, { global: { fetch: fetch }, realtime: { transport: ws } }); async function checkAuth() { const { data, error } = await supabase.auth.admin.listUsers(); console.log(data); if (error) console.error(error); } checkAuth();
