import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; import ws from 'ws'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY, { global: { fetch: fetch }, realtime: { transport: ws } }); async function checkGrandfathered() { const { data, error } = await supabase.from('users').select('handle, is_grandfathered, subscription_tier, email'); console.log('Users:', JSON.stringify(data, null, 2)); if (error) console.error(error); } checkGrandfathered();
